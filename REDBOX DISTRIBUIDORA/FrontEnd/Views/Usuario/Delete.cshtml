@model FrontEnd.Models.UsuarioViewModel

@{
    ViewData["Title"] = "Eliminar Usuario";
}


@* Dando Formato*@
    <div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">

                <h4 class="card-title">Eliminar un Usuario</h4>
                <p class="card-title-desc">
                    A continuación se muestran algunos detalles del usuario a eliminar:                     
                </p>

                <div class="table-responsive">
                    <table class="table table-nowrap mb-0">
                        <thead>
                            <tr>
                                <th style="width: 50%;">Aspecto</th>
                                <th>Detalle</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>@Html.DisplayNameFor(model => model.NombreUsuario)</td>
                                <td>
                                    <a href="#" id="inline-username">@Html.DisplayFor(model => model.NombreUsuario)</a>
                                </td>
                            </tr>
                            <tr>
                                <td>@Html.DisplayNameFor(model => model.Nombre)</td>
                                <td>
                                    <a href="#" id="inline-username">@Html.DisplayFor(model => model.Nombre)</a>
                                </td>
                            </tr>
                           
                        </tbody>
                    </table>
                </div>

                <form asp-action="Delete">
                    @Html.HiddenFor(Model=>Model.IdUsuario)
                    <input type="submit" value="Eliminar Usuario" class="btn btn-danger" />
                    <a asp-action="Index">Volver a la Lista</a>
                </form>

            </div>
        </div>
    </div> <!-- end col -->
</div>
